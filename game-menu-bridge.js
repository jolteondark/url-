import { createSafariPlayableRuntime, loadSafariPlayableRun } from "./runtime/safari-playable-integration.js";

const byId=(id)=>document.getElementById(id);let active="party";const moneyFormat=new Intl.NumberFormat("ja-JP");
function snapshot(){const fallback=createSafariPlayableRuntime();try{const loaded=loadSafariPlayableRun(window.localStorage,fallback);return loaded.found?loaded.state:fallback}catch(_){return fallback}}
function bagSlots(runtime){const slots=Array.isArray(runtime?.bag?.slots)?runtime.bag.slots:[];const totals=new Map();for(const slot of slots){if(!slot)continue;const id=Array.isArray(slot)?slot[0]:slot.id;const qty=Number(Array.isArray(slot)?slot[1]:slot.quantity??0);if(!id||qty<=0)continue;totals.set(id,(totals.get(id)??0)+qty)}return [...totals].sort((a,b)=>String(a[0]).localeCompare(String(b[0])))}
function renderBag(){const pane=byId("menu-bag-pane");if(!pane)return;const runtime=snapshot();const rows=bagSlots(runtime);const wrap=document.createElement("section");wrap.className="card bag-menu-card";const head=document.createElement("div");head.className="section-heading";head.innerHTML='<h2>Bag</h2><span class="pill">INVENTORY</span>';const money=document.createElement("div");money.className="bag-money";const moneyLabel=document.createElement("span");moneyLabel.textContent="所持金";const moneyValue=document.createElement("strong");moneyValue.textContent=moneyFormat.format(Number(runtime?.bag?.money??0))+"円";money.append(moneyLabel,moneyValue);const grid=document.createElement("div");grid.className="bag-grid";if(rows.length){for(const [id,qty] of rows){const row=document.createElement("article");row.className="bag-slot";const name=document.createElement("strong");name.textContent=id;const amount=document.createElement("span");amount.textContent="×"+qty;row.append(name,amount);grid.append(row)}}else{const empty=document.createElement("div");empty.className="bag-empty";empty.textContent="バッグは空です";grid.append(empty)}wrap.append(head,money,grid);pane.replaceChildren(wrap)}
function adoptPanels(){const party=byId("party-detail-card"),box=byId("storage-detail-card"),partyPane=byId("menu-party-pane"),boxPane=byId("menu-box-pane");if(party&&partyPane&&party.parentElement!==partyPane)partyPane.append(party);if(box&&boxPane&&box.parentElement!==boxPane)boxPane.append(box)}
function show(tab){active=tab;adoptPanels();const menu=byId("game-menu");if(!menu)return;menu.hidden=false;document.body.classList.add("menu-open");for(const pane of menu.querySelectorAll("[data-menu-pane]"))pane.hidden=pane.dataset.menuPane!==tab;for(const button of menu.querySelectorAll("[data-menu-tab]"))button.classList.toggle("active",button.dataset.menuTab===tab);byId("game-menu-title").textContent=tab==="party"?"Party":tab==="bag"?"Bag":"Box";if(tab==="bag")renderBag();window.dispatchEvent(new CustomEvent("safari-game-menu-opened",{detail:{tab}}))}
function close(){const menu=byId("game-menu");if(menu)menu.hidden=true;document.body.classList.remove("menu-open")}
new MutationObserver(adoptPanels).observe(document.body,{childList:true,subtree:true});adoptPanels();
byId("menu-party")?.addEventListener("click",()=>show("party"));byId("menu-bag")?.addEventListener("click",()=>show("bag"));byId("menu-box")?.addEventListener("click",()=>show("box"));byId("game-menu-close")?.addEventListener("click",close);byId("game-menu")?.addEventListener("click",(event)=>{const tab=event.target.closest("button[data-menu-tab]");if(tab)show(tab.dataset.menuTab);else if(event.target===byId("game-menu"))close()});document.addEventListener("keydown",(event)=>{if(event.key==="Escape"&&!byId("game-menu")?.hidden)close()});window.addEventListener("storage",()=>{if(active==="bag"&&!byId("game-menu")?.hidden)renderBag()});window.addEventListener("safari-runtime-changed",()=>{if(active==="bag"&&!byId("game-menu")?.hidden)renderBag();adoptPanels()});
